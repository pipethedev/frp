# Test config for frps with dashboard
bindPort = 17000

# Dashboard settings
webServer.addr = "0.0.0.0"
webServer.port = 7500
webServer.user = "admin"
webServer.password = "admin"

# Optional: Tailscale support (if you have it configured)
# useTailscale = false
